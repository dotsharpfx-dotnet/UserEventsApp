# name: Build and Test

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest
    
#     strategy:
#       matrix:
#         dotnet-version: ['10.0.x']

#     steps:
#     - uses: actions/checkout@v4
#       with:
#         fetch-depth: 0

#     - name: Setup .NET
#       uses: actions/setup-dotnet@v4
#       with:
#         dotnet-version: ${{ matrix.dotnet-version }}

#     - name: Restore dependencies
#       run: dotnet restore UserEventsApp.sln

#     - name: Build
#       run: dotnet build UserEventsApp.sln --configuration Release --no-restore

#     - name: Run tests
#       run: dotnet test UserEventsApp.sln --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"

#     - name: Upload test results
#       if: always()
#       uses: actions/upload-artifact@v4
#       with:
#         name: test-results
#         path: '**/test-results.trx'
#         retention-days: 30

#     - name: Publish test results
#       if: always()
#       uses: EnricoMi/publish-unit-test-result-action@v2
#       with:
#         files: '**/test-results.trx'
#         check_name: Test Results
